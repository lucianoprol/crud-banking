<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml" />
  
  <springProperty name="application_name" source="spring.application.name" />
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}) %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint}[TraceID: %X{traceId}] %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}</pattern>
    </encoder>
  </appender>
  <appender name="FILE-ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>C:/appvol/APP/mdw/logs/${application_name}/${application_name}-${HOSTNAME}.log</file>
    
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <filenamePattern>/appvol/APP/mdw/logs/${application_name}/${application_name}-${HOSTNAME}.%d{yyyy-MM-dd-HH}_%i.log.zip</filenamePattern>
      <maxFileSize>50MB</maxFileSize>
      <maxHistory>200</maxHistory>
      <totalSizeCap>500MB</totalSizeCap>
    </rollingPolicy>
    
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}- [%thread] %-5level %logger{36}[TraceID: %X{traceId}] - %replace(%msg){'\n', ''}%n</Pattern>
    </encoder>
  </appender>
  
  
  <root level="INFO">
    <appender-ref ref="STDOUT"/>
    <appender-ref ref="FILE-ROLLING"/>
  </root>
</configuration>